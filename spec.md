# 仕様書：参考画像ベース LUT ジェネレーター（仮称：LUT Match）

- 版数: v0.1（ドラフト）
- 最終更新: 2026-07-09
- ステータス: レビュー待ち

---

## 1. 概要

### 1.1 目的

「元画像（Source）」と「参考画像（Reference）」の2枚を入力すると、元画像の色を参考画像の雰囲気に近づける 3D LUT を自動生成し、`.cube` ファイルとして書き出せる Web アプリを開発する。

例：デジタルカメラで普通に撮った写真（Source）と、昔フィルムカメラで撮った写真（Reference）を入力すると、フィルム風の色合いになる LUT が得られる。

### 1.2 コンセプト

- **完全ローカル処理**：画像はサーバーに一切送信しない。すべてブラウザ内（JavaScript / Web Worker）で計算する
- **自動＋手動**：自動カラーマッチで 8 割を作り、手動パラメーターで追い込む
- **汎用出力**：生成した LUT は DaVinci Resolve / Premiere Pro / Photoshop / Lightroom（プロファイル経由）/ OBS など `.cube` 対応ソフトでそのまま使える

### 1.3 スコープ外（v1 では実装しない）

- 動画ファイルの入力
- グレイン・ビネット等の空間エフェクト（LUT は色の写像のみで原理的に含められないため）
- サーバーサイド処理、アカウント機能、課金
- プリセット保存、PNG HALD 出力（将来検討 → §12）

---

## 2. 用語

| 用語 | 意味 |
|---|---|
| Source | LUT を適用したい元画像 |
| Reference | 目標とする雰囲気の参考画像 |
| 3D LUT | RGB 入力色 → 出力色の 3 次元ルックアップテーブル |
| `.cube` | Adobe/IWLTBAP 系で標準的な LUT テキスト形式 |
| MKL | Monge-Kantorovich 線形化。線形変換による色分布マッチ |
| HM | ヒストグラムマッチング。チャンネル毎の累積分布を一致させる非線形マッチ |
| Identity LUT | 入力＝出力の無変換 LUT。強度ミックスの基準に使う |

---

## 3. 技術スタック・インフラ

| 項目 | 採用 | 理由 |
|---|---|---|
| ビルド | Vite + TypeScript（フレームワークなし・Vanilla TS） | 単一 HTML の身軽さに近い構成を保ちつつ、アルゴリズム・i18n・UI をモジュール分割して保守できる。ビルド成果物は静的ファイルのみ |
| UI | 素の DOM + CSS（ライブラリ不使用） | 依存最小化。画面は実質 1 枚なのでフレームワーク不要 |
| 重い計算 | Web Worker | メインスレッドをブロックせず UI を固まらせない |
| プレビュー描画 | WebGL（3D テクスチャ or 2D パック LUT）/ 非対応時は Canvas 2D + trilinear 補間にフォールバック | スライダー操作にリアルタイム追従させるため |
| リポジトリ | GitHub | ユーザー指定 |
| ホスティング | Cloudflare Pages（GitHub 連携の自動デプロイ、`vite build` → `dist/`） | ユーザー指定。静的サイトなので無料枠で完結 |
| CI | GitHub Actions：push 時に `tsc --noEmit` + ユニットテスト実行 | アルゴリズムの回帰防止 |

外部ランタイム依存（CDN 読み込み等）はゼロにする。devDependencies のみ（vite / typescript / vitest）。

---

## 4. 機能要件

### 4.1 画像入力

- Source / Reference の 2 つのドロップゾーン。ドラッグ＆ドロップとファイル選択の両対応
- 対応形式：JPEG / PNG / WebP（ブラウザがデコードできるもの）
- 入力画像は解析用に長辺 **512px** へ縮小して統計計算に使用（プレビュー表示用は長辺 2048px まで別途保持）
  - 根拠：MKL（平均・共分散）は数千 px、HM（256 ビン CDF）は長辺 256px 相当で概ね足りるが、シャドウ・ハイライトの分布の裾の推定安定性のため余裕を持たせる。処理時間の支配項は元画像のデコード/リサイズであり、512px の統計計算コストは数十 ms と無視できる。内部定数 `ANALYSIS_LONG_EDGE` として変更可能にする
- 透過 PNG はアルファ 50% 未満の画素を統計計算から除外する
- 広色域画像（Display P3 等）は Canvas 経由で sRGB に変換して扱う（制限としてヘルプに明記）
- EXIF の回転を反映する
- 画像差し替え時は自動で再計算する

### 4.2 自動カラーマッチ（3 モード）

| モード | UI 表示名（日/英） | 内部処理 | 想定用途 |
|---|---|---|---|
| A | ナチュラル / Natural | MKL のみ | 破綻を避けたい・被写体が大きく異なる場合 |
| B | 忠実 / Faithful | HM のみ（チャンネル別） | トーンカーブごと強く寄せたい場合 |
| C（既定） | バランス / Balanced | HM → MKL → HM の複合パイプライン | 通常はこれ。非線形の再現と安定性の両立 |

- モード変更時は自動で再計算（Web Worker、目標 1 秒以内）
- 計算は知覚均等性のため **リニア化した RGB** 上で行う（sRGB EOTF でデガンマ → 計算 → 再ガンマ）。HM の輝度成分の暴れが大きい場合に備え、実装時に Lab 空間案と比較検証する（§11 テスト参照）

### 4.3 LUT 生成

1. §4.2 の変換関数 f(r,g,b) を得る
2. N×N×N（既定 33）の格子点すべてに f を適用して 3D LUT 化
3. **平滑化**：3D ガウシアンぼかし（強度スライダー連動）でバンディング・色飛びを抑制
4. **範囲外挙動**：入力画像に存在しない色域（統計の外挿域）は Identity 方向へ減衰させ、極端な色化けを防ぐ

### 4.4 手動調整パラメーター

生成された LUT に対して、以下を LUT に「焼き込む」形で追加調整できる。すべてリアルタイムでプレビューに反映。

| パラメーター | 範囲 | 既定値 | 備考 |
|---|---|---|---|
| 強度（ミックス） | 0–100% | 85% | Identity LUT とのブレンド率 |
| スムージング | 0–100 | 20 | §4.3 の平滑化強度 |
| 露出 | −2.0〜+2.0 EV | 0 | |
| コントラスト | −50〜+50 | 0 | 中間グレー基準の S カーブ |
| 彩度 | −100〜+100 | 0 | |
| 色温度 | −100〜+100 | 0 | Lab の b* 軸相当 |
| ティント | −100〜+100 | 0 | Lab の a* 軸相当 |
| ブラック保護 | 0–20% | 5% | 指定輝度以下を統計マッチから除外し、締まった黒を維持 |

- 「リセット」ボタンで手動調整のみ初期化（自動マッチ結果は保持）

### 4.5 プレビュー

- Source に現在の LUT（自動＋手動込み）を適用した結果を常時表示
- **ビフォーアフター比較スライダー**：1 枚の画像の上で境界線をドラッグして適用前/後を左右比較（初期位置 50%）
- 表示切替タブ：`元画像` / `適用後` / `比較スライダー`（既定）
- Reference はサムネイルとして常時参照表示
- 100% 表示とフィット表示の切替、ピンチ/ホイールズーム

### 4.6 書き出し

- 形式：`.cube`（3D LUT）
- サイズ選択：**17 / 33 / 65**（既定 33）
- 出力内容＝自動マッチ＋手動調整をすべて合成した最終 LUT
- ファイル名：ユーザー入力（既定 `lutmatch_YYYYMMDD_HHmm.cube`）。名前は `TITLE` ヘッダーにも埋め込む
- 併せて「適用後画像を PNG 保存」ボタンも用意（確認用）

### 4.7 UI 言語（日英切替）

- 日本語 / English の 2 言語。ヘッダーのトグルで即時切替
- 初期言語はブラウザの `navigator.language` から自動判定（`ja*` → 日本語、それ以外 → 英語）
- 選択は `localStorage` に保存
- 文言は `src/i18n/ja.ts` / `en.ts` の辞書オブジェクトで一元管理

---

## 5. アルゴリズム仕様

### 5.1 前処理

1. 画像を長辺 512px に縮小、ピクセル配列を取得
2. sRGB → リニア RGB へ変換
3. ブラック保護しきい値未満の画素を統計対象から除外

### 5.2 MKL（モード A）

Source / Reference それぞれの平均ベクトル μ と共分散行列 Σ を計算し、閉形式解

T = Σs^(-1/2) · (Σs^(1/2) · Σr · Σs^(1/2))^(1/2) · Σs^(-1/2)

により線形写像 `f(x) = T(x − μs) + μr` を得る（Pitié & Kokaram 2007）。3×3 の固有値分解のみで済むため軽量。

### 5.3 HM（モード B）

R/G/B 各チャンネルについて Source の累積分布関数を Reference の累積分布関数へ写像（256 bin、単調性を保証）。得られる 3 本の 1D カーブを 3D 格子へ適用する。

**ビン割りはガンマ（sRGB）空間で等間隔に定義する。** リニア空間で等間隔ビンを切るとシャドウ側の階調が極端に粗くなり、フィルムルックで重要な暗部の再現が破綻するため。リニア値はビン参照時にガンマ空間へ写像して扱う。

### 5.4 複合（モード C・既定）

`HM → MKL → HM` の直列適用（color-matcher の HM-MVGD-HM 構成に準拠、線形段は MKL を使用）。各段は §5.2 / §5.3 の実装を再利用する。

**中間段では 0–1 クランプを行わない**（範囲外値を保持したまま次段へ渡す）。段ごとにクランプすると情報が失われ、複合の利点が損なわれるため。クランプは §5.5 の最終ステップのみ。

### 5.5 LUT 化と後段

1. N³ 格子点に f を適用
2. 外挿域の Identity 減衰：Source 色分布の平均・共分散に基づく**マハラノビス距離**がしきい値 d0 を超える格子点は、距離に応じて f の出力を Identity 方向へ線形に減衰させる（d0 と減衰幅は実装時にチューニング、既定 d0=3.0）
3. 3D ガウシアン平滑化
4. 手動調整（露出→色温度/ティント→コントラスト→彩度 の順で適用）
5. Identity とのミックス（強度）
6. 0–1 にクランプ

### 5.6 .cube 出力形式

```
TITLE "<ユーザー指定名>"
LUT_3D_SIZE 33
DOMAIN_MIN 0.0 0.0 0.0
DOMAIN_MAX 1.0 1.0 1.0
<r g b>  # R が最も速く回る順序、小数 6 桁
```

TITLE に埋め込む文字列は互換性のため**サニタイズ**する：ダブルクォートを除去し、印字可能 ASCII 以外（日本語等）は `_` に置換または除去。サニタイズ後に空になる場合は既定名を使用。ダウンロードファイル名自体は日本語可。

---

## 6. UI 仕様

### 6.0 設計原則

1. **ステップは見せる、ウィザードにはしない**：ヘッダーに「1 画像を選ぶ → 2 調整 → 3 書き出し」の進行インジケーターを表示（現在地をハイライト）。ただし画面分割はせず全機能を 1 画面に置く。調整⇄比較を行き来する使い方を妨げないため
2. **プレビュー常時可視**：パラメーター操作中、結果が必ず画面内に見えていること（本アプリ最重要の UX 要件）
3. **調整は 2 階層**：常時表示は「モード 3 択＋強度スライダー」のみ。露出以下 7 項目は「詳細調整」アコーディオン（既定：閉）に格納し、初見の認知負荷を下げる
4. **書き出し導線は固定**：書き出しバーは常に画面内（デスクトップ：プレビュー直下、モバイル：画面下部 sticky）

### 6.1 ブレークポイントとレイアウト

| 区分 | 幅 | レイアウト |
|---|---|---|
| モバイル | < 768px | 1 カラム縦積み＋2 つの sticky（下記） |
| タブレット | 768–1119px | 2 カラム（左レール 240px ＋ プレビュー） |
| デスクトップ | ≥ 1120px | 2 カラム（左レール 280px ＋ プレビュー）、余白拡大 |

**デスクトップ / タブレット（2 カラム）**

- 左レール（上から）：Source / Reference サムネイル（横並び 2 枚、クリックで差し替え）→ モード 3 択（セグメントコントロール）→ 強度スライダー → 「詳細調整」アコーディオン → リセット。レール自体が縦スクロール
- 右カラム：プレビュータブ（元画像 / 適用後 / 比較〔既定〕）→ 比較スライダー付きプレビュー（`position: sticky` でスクロールに追従）→ 直下に書き出しバー（サイズ選択 / ファイル名 / DL ボタン）
- 参考画像のサムネイルをプレビュー右上に小窓表示（タップで一時拡大）し、「目標」を常に参照できるようにする

**モバイル（1 カラム）**

- 上から：ヘッダー（ロゴ / JA|EN / ?）→ 入力カード 2 枚（横並び）→ **プレビュー（画面上部に sticky）** → モード 3 択 → 強度 → 詳細調整アコーディオン → **書き出しバー（画面下部に sticky）**
- プレビューを上部 sticky にすることで、下のスライダーをスクロール操作している間も結果が見え続ける（原則 2 の実現手段）
- sticky プレビューの高さは画面高の最大 40% にクランプし、スライダー操作領域を確保する

### 6.2 状態と遷移

1. **初期（空状態）**：ドロップゾーン 2 つと「サンプル画像で試す」ボタンのみ有効。調整・書き出しはグレーアウト。サンプルボタンは同梱のデモペア（public/）を読み込み、初見ユーザーが 10 秒で全フローを体験できるようにする
2. **片方のみ投入**：もう一方のドロップゾーンを点滅等で誘導。進行インジケーターは「1」のまま
3. **計算中**：プレビュー領域にスケルトン＋プログレス（Worker からの進捗イベント）。UI 操作は可能
4. **生成済み**：全機能有効。パラメーター変更は即時反映、モード変更・画像差し替えは再計算。進行インジケーターは「2 調整」へ
5. **エラー**：非対応形式・デコード失敗・巨大画像（>50MP）はトースト表示で通知し、直前の状態を維持

### 6.3 インタラクション詳細

- **比較スライダー**：デスクトップは境界線上どこでもドラッグ可＋ハンドル。**モバイルはハンドル（直径 44px 以上）のドラッグのみで動作**させ、縦スクロールとのジェスチャー競合を回避する。キーボード（←→）対応
- **スライダー全般**：タッチターゲット 44px 以上。数値ラベルをタップすると直接数値入力できる。ダブルタップ / ダブルクリックで個別リセット
- **プレビュー**：ダブルタップ / ホイールで 100%⇄フィット切替、ピンチズーム対応
- **書き出しバー**：DL ボタンは常時 1 つのプライマリボタン。ファイル名・サイズ選択はモバイルではボタン長押し（またはバー内の展開）で表示し、既定値ならワンタップで保存完了

### 6.4 補助 UI・ビジュアル

- ダークテーマ基調（色評価を妨げないニュートラルグレー背景）。UI のアクセント色は彩度低めに抑え、プレビューの色を邪魔しない
- 各スライダーにツールチップで一言説明（i18n 対象）
- `?` ボタンで使い方モーダル（3 ステップ図解＋「LUT が扱える範囲」の注記）
- 「Source と Reference の被写体が近いほど良い結果になる」旨のヒントを初回表示

---

## 7. 非機能要件

| 項目 | 目標 |
|---|---|
| 自動マッチ計算時間 | 1 秒以内（512px 解析、一般的なノート PC） |
| スライダー応答 | 16ms 目標 / 最悪 100ms 以内にプレビュー反映（WebGL 経路） |
| プライバシー | 画像・生成 LUT を外部送信しない。アナリティクスを入れる場合も画像データは対象外と明記 |
| 対応ブラウザ | Chrome / Edge / Firefox / Safari の最新 2 メジャーバージョン。**モバイル（iOS Safari / Android Chrome）はデスクトップと同等の一級対応** |
| アクセシビリティ | スライダーはキーボード操作可、コントラスト比 WCAG AA |
| オフライン | ページを開いたまま（リロードしない限り）はネットワーク不要で全機能動作。完全オフライン対応（Service Worker / PWA）は将来検討 |

---

## 8. ディレクトリ構成（案）

```
/
├─ index.html
├─ vite.config.ts
├─ src/
│  ├─ main.ts              # エントリ・UI 結線
│  ├─ ui/                  # ドロップゾーン、スライダー、比較スライダー
│  ├─ core/
│  │  ├─ colorspace.ts     # sRGB↔リニア、Lab 変換
│  │  ├─ mkl.ts            # §5.2
│  │  ├─ histmatch.ts      # §5.3
│  │  ├─ pipeline.ts       # モード合成・§5.5 後段
│  │  ├─ lut.ts            # 格子生成、trilinear 補間、平滑化
│  │  └─ cube.ts           # .cube シリアライズ
│  ├─ worker/match.worker.ts
│  ├─ gl/preview.ts        # WebGL プレビュー（+ Canvas フォールバック）
│  └─ i18n/{ja,en}.ts
├─ tests/                  # vitest（core/ を対象）
└─ public/                 # サンプル画像（デモ用・権利クリアなもの）
```

---

## 9. 開発マイルストーン

| M | 内容 | 完了条件 |
|---|---|---|
| M1 | コア実装：MKL・HM・LUT 化・.cube 出力（UI 最小） | 2 枚入力 → .cube DL が通しで動く |
| M2 | 3 モード＋手動調整＋Worker 化 | 全パラメーターがプレビューに反映される |
| M3 | 比較スライダー・WebGL プレビュー・i18n | §4.5 / §4.7 充足 |
| M4 | エラー処理・モバイル対応・ヘルプ・仕上げ | §6.2 / §7 充足 |
| M5 | GitHub Actions CI + Cloudflare Pages 自動デプロイ | main への push で本番反映 |

---

## 10. リスクと対策

| リスク | 対策 |
|---|---|
| HM 系モードで被写体差により色が暴れる | 既定を複合モード＋強度 85% にする。ブラック保護と Identity 減衰。ヒント表示で運用面もカバー |
| バンディング | 3D 平滑化（既定 ON）。書き出しサイズ 65 の選択肢 |
| Safari の WebGL/メモリ制約 | Canvas 2D フォールバック経路を必ず維持。解析解像度は 512px 固定 |
| LUT では再現できない要素（粒状感等）への期待値ギャップ | ヘルプに「LUT が扱える範囲」を明記 |

---

## 11. テスト方針

- **ユニット**：色空間変換の往復誤差、MKL の閉形式解（既知の共分散で検証）、HM の単調性、.cube のフォーマット妥当性
- **ゴールデンテスト**：固定入力ペアに対する LUT 出力のスナップショット比較（アルゴリズム回帰検知）
- **同一性テスト**：Source＝Reference のとき LUT ≒ Identity になること
- **実写検証**：デジタル写真×フィルム写真のペア数組で目視評価。リニア RGB 処理と Lab 処理の比較検証もここで実施（§4.2）
- **外部互換**：書き出した .cube を DaVinci Resolve / Photoshop で読み込み、ブラウザプレビューと一致することを確認

---

## 12. 将来拡張（バックログ）

- PNG HALD 形式出力
- 設定・LUT のプリセット保存（localStorage / ファイル）
- 動画フレーム抽出入力
- グレイン等のプレビュー専用エフェクト（LUT には含まれない旨明示）
- PWA 化（オフラインインストール）
- 複数 Reference のブレンド

---

## 13. 決定事項ログ

| 日付 | 決定 | 経緯 |
|---|---|---|
| 2026-07-09 | スタック：Vite + TypeScript（フレームワークなし） | 単一 HTML 志向を尊重しつつ保守性を確保 |
| 2026-07-09 | アルゴリズム：3 モード（MKL / HM / 複合、既定＝複合） | フィルム再現に非線形が必要、複合の追加コストが小さいため |
| 2026-07-09 | 初期機能：日英 UI 切替、比較スライダーを含む | ユーザー選択 |
| 2026-07-09 | プリセット保存・PNG HALD は v1 スコープ外 | ユーザー選択（バックログへ） |
| 2026-07-09 | 解析解像度は長辺 512px を維持（内部定数化） | 分布の裾の安定性を優先。縮小しても速度向上がほぼないため（レビューで再確認） |
| 2026-07-09 | HM のビン割りはガンマ空間、複合パイプラインは中間クランプなし、外挿減衰はマハラノビス距離ベース、TITLE は ASCII サニタイズ | 仕様レビューでの修正 |
| 2026-07-09 | UI を再設計：進行インジケーター付き 1 画面構成、プレビュー sticky（デスクトップ右カラム / モバイル上部）、調整 2 階層、書き出しバー固定、サンプル画像の空状態、モバイル一級対応 | レスポンシブ・UX 見直し |

---

## 14. 未決事項（要オーナー判断）

| # | 項目 | 選択肢 | 備考 |
|---|---|---|---|
| 1 | OSS ライセンス | MIT / Apache-2.0 / 非公開（ライセンスなし） | 公開リポジトリなら明記推奨。迷う場合は MIT が無難 |
| 2 | アクセス解析 | 入れない（完全ノートラッキングを明示） / Cloudflare Web Analytics（Cookie なし） | 画像データは§7 によりいずれも送信されない |